About orchestrates Bold Frame Media's manifesto after DomeGallery completes its kinetic spin. This section spans full viewport height, layering textual proclamations with a Three.js tunnel titled Tube. The hero grid invites visitors into a cinematic corridor where lines like WE SCULPT SENSORIAL EXPERIENCES appear sequentially. Each phrase sits within `heroLines`, letting designers adjust copy without touching markup. The container uses `min-h-screen`, heavy padding, plus `text-[clamp(3rem,9vw,11rem)]` to maintain colossal typography across breakpoints. Colors remain mostly monochrome, enabling highlight overlays to shine whenever secondary narratives trigger.

GSAP carries the narrative weight via `useEffect`. The hook registers ScrollTrigger globally, then builds `introTimeline` to fade each hero line upward with slight offsets. Another timeline named `swapTimeline` attaches to ScrollTrigger with start `top top` plus end `+=200%`, giving plenty of scroll distance for multiple copy swaps. Utility helpers `sequenceLinesIn` plus `sequenceLinesOut` accept arrays of paragraph refs, letting each overlay group fade with identical cadence. That design keeps animation definitions concise while still enabling distinct color palettes per group.

Four overlay group refs — `heroPrimaryGroupRef`, `heroSecondaryGroupRef`, `heroTertiaryGroupRef`, `heroQuaternaryGroupRef` — describe successive statements. Each group sits absolutely within the hero container, pinned via `pointer-events-none` to prevent interference with Tube. Secondary copy highlights Evolution in Motion with crimson text, tertiary copy emphasises Memory Architecture using white accents, final copy invites collective ascent with rose gradients. Because GSAP toggles `opacity`, `yPercent`, plus `scale`, these groups appear sequentially without reflow, letting visitors feel like captions drift across the tunnel interior.

The text itself originates from arrays `heroLines`, `evolutionLines`, `resonanceLines`, `horizonLines`. Each array defines three uppercase statements, meaning the component can swap vocabulary by editing a single constant. Refs populate inside `map` loops via callback functions that push nodes into arrays like `heroRefs.current[index] = el`. That manual indexing ensures GSAP timelines reference actual DOM nodes even after React re-renders. Additional arrays `statRefs` plus `panelRefs` remain for future metric blocks, though those sections stay commented for now, hinting at expansion without cluttering the current viewport.

Tube contributes a cinematic wormhole behind the copy. The helper receives `sectionRef` from About, rendering a Three.js scene pinned through ScrollTrigger. Within Tube a CatmullRom curve feeds TubeGeometry while UnrealBloomPass overlays luminous highlights. GSAP timeline drives `tubePerc.percent` from zero to ninety-six percent across a two-hundred percent scroll segment, mapping that value to camera travel along the curve. Particles drift within multiple BufferGeometries, giving depth cues. Event listeners manage resize, mousemove, plus cleanup inside the effect teardown, keeping GPU usage under control.

Below the hero, About transitions into a content grid padded by `px-6 md:px-12 lg:px-20`. Many sections remain disabled, yet scaffolding demonstrates how stats alongside journey panels could animate into view via ScrollTrigger watchers that already exist. The marquee near the end stays active: a rounded-full container uses border plus blur styles while inline keyframes defined via styled-jsx translate the text row. `marqueeWords` holds phrases like Immersion, Sensory design, Photon bloom, each duplicated to maintain seamless motion without visible seams.

Structural decisions keep the section integrated with the broader landing page. The component exports default function About, consumed within `app/page.tsx` inside `<section id="about">`. Internally the root `<section>` uses `id="about-section"`, giving two anchor targets. The hero area stays `min-h-screen`, guaranteeing Tube plus text fill the viewport even on tall displays. Overflow remains hidden to mask GSAP translations beyond the edges, while `relative isolate` ensures z-index layering works alongside background glows elsewhere on the page.

Performance remains manageable despite the visuals because heavy operations live inside GSAP contexts that revert during cleanup. `gsap.context` scopes selectors to `sectionRef`, preventing stray animations from targeting identical class names elsewhere on the site. When the component unmounts, `ctx.revert()` resets inline styles, leaving DOM clean for Next.js fast navigation. That habit proves crucial for App Router transitions where sections mount frequently. With these contexts, ScrollTrigger watchers detach immediately, reducing memory pressure during long browsing sessions.

Even though numerous data arrays stay commented, their presence illustrates a plan for scaling storytelling without rewriting behavior. Stats block, journey panels, plus manifesto cards already include markup with `rounded` surfaces, uppercase tokens, plus `backdrop-blur` touches consistent with Bold Frame Media branding. React refs pointing at those nodes still fill `statRefs` plus `panelRefs`, enabling GSAP to target them the moment the comments get lifted. Through this structure, About remains both theatrical for current release yet flexible for future experiments.
