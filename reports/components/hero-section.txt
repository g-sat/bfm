HeroSection opens the narrative experience immediately after Navbar, filling the first viewport with a multisensory tableau. The file declares 'use client', revealing reliance on pointer handling plus downstream client components. Imports bring Hero3D, HeroText, plus ProximityHeroFrames, which together stage the volumetric sculpture, typographic reveal, plus reactive frames. No props enter this component; it functions as a director orchestrating child modules while controlling the spatial arrangement through utility classes.

The top-level <section className="relative min-h-screen"> establishes a canvas that always spans at least one viewport height. Inside, a background wrapper uses absolute positioning, negative z-index, overflow-hidden, plus pointer-events-none to guarantee purely decorative behavior. The gradient stack mixes a vertical linear gradient with three radial glows positioned at corners, each blurring between 160px plus 200px radii. These definitions mimic stage lighting, bathing the hero in chromatic fog while staying GPU friendly because they rely on CSS only.

A second container <div className="relative flex items-center justify-center min-h-screen h-screen overflow-hidden"> houses the interactive payload. This wrapper ensures the layout remains centered even during window resizes. It includes another radial blur set slightly above center, giving depth behind the 3D model. Setting pointer-events-none on this background ensures that pointer capture belongs to the child layers rather than the decorative glows.

ProximityHeroFrames occupies the full inset within a pointer-enabled absolute layer. The prop className="h-full w-full" keeps the grid spanning the viewport. Although the internal implementation lives elsewhere, this wrapper ensures the interactive frames track pointer movement even when the user hovers near the viewport edges. Because the component sits above the gradient stack yet below the textual column, the frames respond to motion while ceding final focus to hero copy.

The main content column uses <div className="relative w-full max-w-[80vw] h-screen flex flex-col">, giving plenty of breathing room on large monitors. Inside, the upper region grows to fill space, letting the 3D model plus typography float. Hero3D sits inside an absolutely positioned container anchored to the right third (`w-1/3`). The wrapper sets pointer-events-none because Hero3D handles its own detection via global mouse listeners rather than local hits, preventing scroll interference near the canvas boundary.

HeroText occupies the remaining area through another absolute inset container with left padding. That placement keeps the heading closer to the viewer while the model floats behind. HeroText handles GSAP-driven letter reveals plus CTA hover physics. Because HeroSection leaves pointer events disabled for this wrapper, HeroText opts into pointer control only where necessary: the CTA button toggles pointer-events-auto so clicks register, while other spans remain inert to avoid blocking gestures directed at ProximityHeroFrames.

Below the floating layer, a supportive copy block uses `<div className="relative z-15 pointer-events-none flex flex-col md:flex-row md:items-end justify-between gap-8 pl-16 pb-12 text-white">`. This area includes a tagline paragraph stack plus a CTA cluster. Typography uses uppercase tracking for the label, a bold h2 describing adaptive soundscapes, plus descriptive body copy. On the right, a button labeled "Enter the Dome" invites interaction. The button re-enables pointer events, uses rounded borders, translucent background, plus hover transitions that lighten the stroke.

A vertical list of capability labels follows the button, showcasing "Spatial Audio", "Volumetric Light", plus "Reactive Installations". Each span uses consistent tracking to link back to the brand typography language. Because pointer events remain disabled for this list, the user can still drag across the hero without accidental text selection, yet the call-to-action button remains responsive thanks to its pointer override.

At the base of the section, a horizontal gradient `div` overlays the transition into the next module. `.absolute.bottom-0.left-0.right-0.h-32` ensures the fade remains fixed even when content above scrolls. The gradient goes from transparent to a subtle navy, softening the edge before DomeGallery takes over. This flourish prevents harsh contrast shifts between hero fog plus the darker gallery background.

Pointer governance remains central inside this section. Almost every decorative wrapper includes pointer-events-none, meaning scroll gestures pass through gradients, blur clouds, plus doc-level overlays. Only interactive children override this setting. ProximityHeroFrames toggles pointer events to capture hover distance, while the CTA button uses pointer-events-auto. This discipline prevents stuck scroll states, especially when users drag across the hero on touch devices. It also reduces accidental selection of heading spans, preserving the cinematic atmosphere.

Even though Hero3D lives in another module, HeroSection anticipates its footprint. The container width limit, top alignment, plus z-index guarantee the canvas never overlaps the call-to-action zone. Hero3D itself sets camera position `[2, 0, 6]`, uses ambient plus directional lights, plus lerps rotation in response to normalized mouse coordinates. Because HeroSection anchors the canvas within a stationary column, the subtle rotation reads as parallax rather than a chaotic spin, matching the calm tone of the copy.

HeroText's animation timeline depends on stable layout metrics. HeroSection supplies that stability via consistent padding, height, plus flex behavior. When HeroText injects span elements for each letter, the surrounding container already enforces `leading-none` plus large font sizes, keeping the letters inside the viewport even on small laptops. The gradient blob button effect uses absolute positioning relative to the anchor; HeroSection's pointer gating ensures no other element intercepts those pointermove events.

Responsiveness also plays out through the `max-w-[80vw]` constraint plus `md:flex-row` settings in the lower panel. On mobile, the stack remains vertical, giving the tagline priority. On larger screens the copy plus CTA align on a single baseline, echoing editorial spreads. The concluding gradient overlapping the next module hides abrupt color shifts when the page scrolls, creating a handoff into DomeGallery that feels intentional rather than jarring. All these touches live within this component, enabling downstream modules to focus solely on their own mechanics.

HeroSection demonstrates Bold Frame Media's choreography mindset: pure layout logic sits here, while specialized behaviors live in focused children like Hero3D or HeroText. No props are accepted, yet the component coordinates z-index layers, pointer policies, plus spatial rhythm. Designers can swap backgrounds or spacing using the class strings without touching animation logic. Developers can introduce new hero widgets by placing them within the established flex scaffolding. The component therefore operates as a resilient stage manager, channeling dramatic visuals through a predictable structure.
